# NodeJS REST API Backend Baseline

A template for a NodeJS based REST API backend

**Requirements**
-  [NodeJS](https://nodejs.org/en/)

## Startup

First install package dependencies via:

```
npm install
```

Then run:

```
npm start
```